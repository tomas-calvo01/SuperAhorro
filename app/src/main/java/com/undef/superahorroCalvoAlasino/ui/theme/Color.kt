package com.undef.superahorroCalvoAlasino.ui.theme

import androidx.compose.ui.graphics.Color

// Colores principales de SuperAhorro
val GreenPrimary = Color(0xFF2E7D32)      // Verde principal (usado en toda la app)
val GreenLight = Color(0xFFE8F5E9)        // Verde claro (indicadores, fondos)
val GreenDark = Color(0xFF1B5E20)         // Verde oscuro (modo oscuro)

// Colores secundarios
val RedError = Color(0xFFD32F2F)          // Rojo para errores
val RedErrorBackground = Color(0xFFFFEBEE) // Fondo de mensajes de error
val GrayBackground = Color(0xFFF5F5F5)    // Fondo gris claro
val GrayText = Color(0xFF757575)          // Texto secundario gris

// Colores base (para textos y fondos)
val BackgroundWhite = Color.White
val TextBlack = Color.Black
val TextGray = Color.Gray

// Colores para el tema claro
val LightPrimary = GreenPrimary
val LightSecondary = GreenLight
val LightTertiary = GreenDark

// Colores para el tema oscuro
val DarkPrimary = GreenLight
val DarkSecondary = GreenPrimary
val DarkTertiary = GreenLight
